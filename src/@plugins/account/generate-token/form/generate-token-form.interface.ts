/**
 * A classe `GenerateTokenFormInterface` é responsável pela estrutura de dados para gerar token de validação de conta.
 */
export interface GenerateTokenFormInterface {
  email: any; //Email do usuário
}
